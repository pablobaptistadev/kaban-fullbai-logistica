# Copia para .env.local e preenche (não commits .env.local).
# Supabase: SQL em database/schema.sql → cola no SQL Editor do projeto.
# Exemplo de URL (substitui pelo teu project ref se for diferente):
VITE_SUPABASE_URL=https://ekkgfwoiddwfjbdwyabj.supabase.co
# Dashboard → Settings → API → anon public (JWT longo)
VITE_SUPABASE_ANON_KEY=
